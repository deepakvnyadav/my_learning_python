color = ["red", "white", "black"]
print(color)
color.append("yellow") # it can be add single value in the list at the end.
color.extend(['green', 'blue', 'Pnik']) # it can be add multiplle value at the end.
del color[0] # it is used for delete from the list using the index.
print(color)
color.clear() #it can be clear list.
print(color)
